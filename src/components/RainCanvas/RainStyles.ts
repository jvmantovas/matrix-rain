import styled from "styled-components";

export const RainContainer = styled.div`
  overflow: hidden;
  background: black;

  canvas {
    background: black;
    position: absolute;
    top: 0;
    left: 0;
  }
`;
